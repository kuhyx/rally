class_name Palette
## The only colours in the game. Every mesh, light tint and HUD element picks
## from here so the whole scene reads as one palette (procedural art, no assets).

const SKY: Color = Color(0.55, 0.75, 0.92)
const GRASS: Color = Color(0.38, 0.56, 0.24)
const TARMAC: Color = Color(0.20, 0.21, 0.23)
const GRAVEL: Color = Color(0.62, 0.52, 0.36)
const CAR: Color = Color(0.85, 0.16, 0.14)
const TREE: Color = Color(0.14, 0.32, 0.16)
const TRUNK: Color = Color(0.36, 0.24, 0.14)
const GATE: Color = Color(0.95, 0.85, 0.20)
const INK: Color = Color(0.08, 0.09, 0.10)
const CREAM: Color = Color(0.95, 0.94, 0.86)
